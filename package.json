{
  "scripts": {
  "heroku-postbuild": "npm i -g @nestjs/cli && npm --prefix back-end/lab-queue install && npm --prefix back-end/lab-queue run build && npm --prefix back-end/lab-queue run start:prod"
  }
}
