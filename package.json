{
  "scripts": {
    "build": "cd back-end/lab-queue && npm i -g @nestjs/cli && npm --prefix back-end/lab-queue install",
    "start:prod": "cd back-end/lab-queue && npm install && npm run start:prod"
  }
}
