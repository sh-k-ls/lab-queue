{
  "scripts": {
    "build": "cd back-end/lab-queue && npm i -g @nestjs/cli && npm install && npm i @types/passport && npm i @types/express && npm i @types/passport-jwt && npm i @types/passport-local && npm run build",
    "start:prod": "cd back-end/lab-queue && npm run start:prod"
  }
}
